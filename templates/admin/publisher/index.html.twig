{% extends 'base.html.twig' %}


{% block title %}Publisher
{% endblock %}

{% block body %}

	<div class="container">
		<h1 class="text-center m-4">Ajouter un éditeur</h1>

		<div class="m-4">
			<a href="{{ path('app_home') }}" class="default-link">Accueil</a>
		</div>

		<div class="m-4">

			{{ form_start(formPublisher) }}
			{{ form_end(formPublisher) }}
		</div>
	</div>
{% endblock %}
